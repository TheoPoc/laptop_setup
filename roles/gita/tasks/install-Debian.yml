---
# Debian/Ubuntu-specific installation for gita

- name: Verify uv is available via mise
  ansible.builtin.command:
    cmd: "mise which uv"
  register: uv_check
  changed_when: false
  failed_when: false
  check_mode: false
  tags: gita
