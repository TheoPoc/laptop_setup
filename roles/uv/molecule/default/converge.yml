---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  vars:
    mise_tools:
      uv: "latest"
  tasks:
    - name: Include uv role
      ansible.builtin.include_role:
        name: uv
